import React from 'react';
import { EncounterCreationClient } from './EncounterCreationClient';

/**
 * Encounter Creation Page - Server Component
 *
 * Provides encounter creation functionality including:
 * - Basic encounter information setup
 * - Participant selection and configuration
 * - Settings and difficulty configuration
 * - Save and navigation options
 */
export default function EncounterCreationPage() {
  return <EncounterCreationClient />;
}